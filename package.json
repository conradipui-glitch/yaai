{
  "name": "yaai-wordstat-batch",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Local Wordstat research UI with intent clustering, query type classification and content actions",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check lib/analyze.mjs && node --check public/app.js && node --check scripts/selftest.mjs && node scripts/selftest.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
