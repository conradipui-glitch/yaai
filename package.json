{
  "name": "yaai-wordstat-batch",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local batch research UI for Yandex Wordstat API with cleanup and intent clustering",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check lib/analyze.mjs && node --check scripts/selftest.mjs && node scripts/selftest.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
